function chocolatefactory(type, shape, flavour ) {
    return {
        type, shape, flavour,
    }
}

const chocolava = chocolatefactory("dark chocolate", "round", "strawberry");
const chocofill = chocolatefactory("while chocolate", "heart", "vanilla");
